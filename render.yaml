services:
  - type: web
    name: spring-experiment
    env: docker
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: production
      - key: PORT
        value: 8080
      - key: SERVER_PORT
        value: 8080
    plan: free